import './Hero.css';

export default function Hero() {
  return (
    <section className="hero" id="hero">
      <div className="container hero__content">
        <h1 className="hero__title">
          PDF Editor
        </h1>

        <p className="hero__subtitle">
          The only free PDF editor that doesn't require login
        </p>
      </div>
    </section>
  );
}

